# Server Environment Variables

# Server
PORT=3000
NODE_ENV=development

# Database
# For local development with Docker: mssql://sa:YourStrong!Passw0rd@localhost:1433/softball?encrypt=false
# For Azure SQL: mssql://username:password@server.database.windows.net:1433/database?encrypt=true
DATABASE_URL=

# Client URL
CLIENT_URL=http://localhost:5173

# Media Storage (relative to workspace root for local dev, absolute for Docker)
# Local dev: local/media
# Docker: /app/media (mounted volume)
MEDIA_PATH=local/media

# CORS Origin (comma-separated list or *)
CORS_ORIGIN=http://localhost:5173

# Logging Level (error, warn, info, debug, verbose)
LOG_LEVEL=info
